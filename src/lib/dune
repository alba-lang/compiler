(library
  (name lib)
  (public_name compiler.lib)
  (libraries
    unix
    fmlib_std
    fmlib_pretty
    fmlib_parse
  )
  (inline_tests)
  (preprocess (pps ppx_inline_test))
)

(documentation
  (package compiler)
)
