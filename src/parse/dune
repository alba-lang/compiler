(library
  (name alba_parse)
  (public_name compiler.parse)
  (libraries fmlib_std fmlib_parse)
  (inline_tests)
  (preprocess (pps ppx_inline_test))
)

(documentation
  (package compiler)
  (mld_files parse parse_docu)
)
