(library
  (name core)
  (public_name compiler.core)
  (libraries fmlib_std fmlib_parse fmlib_pretty alba_parse)
  (inline_tests)
  (preprocess (pps ppx_inline_test))
)


(documentation
  (package compiler)
  (mld_files core core_docu)
)
